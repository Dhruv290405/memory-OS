version: '3.8'
services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - BACKEND=sqlite
    volumes:
      - memoryos-data:/tmp/memoryos

  # Optional Ollama service (commented out by default)
  # ollama:
  #   image: ollama/ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama-data:/root/.ollama

volumes:
  memoryos-data:
  # ollama-data:
